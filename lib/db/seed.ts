/**
 * Demo seed script — no longer used for production.
 * Real courses are created via the admin panel at /admin/courses/new.
 * Run:  npx tsx --env-file=.env.local scripts/make-admin.ts <email> [password]
 */
export {};
